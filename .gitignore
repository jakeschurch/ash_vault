/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
ash_vault-*.tar
/tmp/
.elixir_ls/

# The example app is committed; its build artefacts are not. The entries above
# are root-anchored, so they do not cover the subdirectory.
/example/_build/
/example/deps/
doc/

# nix / direnv
.direnv/
result
result-*

# BEAM crash dumps can contain key material — never commit one.
erl_crash.dump

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
